language: csharp
solution: Alugamer.sln
dotnet: 3.1
mono: none

dist: xenial
services:
  - xvfb

addons:
  browserstack:
    username: "voull1"
    access_key: $BROWSERSTACK_KEY
  chrome: stable

script:
  - dotnet restore
  - dotnet build --configuration Release
  - dotnet test